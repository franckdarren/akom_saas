Crée une migration Prisma pour le changement demandé :

1. Modifie le schema.prisma
2. Génère la migration avec `npx prisma migrate dev --name <nom>`
3. Écris le SQL équivalent pour Supabase
4. Vérifie que le RLS est cohérent pour les nouvelles tables
